spring:
  devtools:
    restart:
      aditional-paths: src/main/java
  application:
    name: arancel-service
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    plataform: postgres
    driver-class-name: org.postgresql.Driver
    dbname: TingesoArancel
    url: jdbc:postgresql://localhost:5432/TingesoArancel
    username: postgres
    password: Paralele34
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081