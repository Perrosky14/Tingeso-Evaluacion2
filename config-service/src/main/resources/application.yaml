server:
  port: 8081
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: main
          uri: https://github.com/Perrosky14/Tingeso-Evaluacion2
          search-paths: "config-data"